# Local: copy to .env, then `docker compose -f docker-compose.dev.yml up -d`
# Dokploy: paste this file into the Environment tab (change WP_HOME / WP_ENV / secrets).
# Compose interpolates this file; Bedrock's phpdotenv loads it at runtime.

DB_NAME='wordpress'
DB_USER='wordpress'
DB_PASSWORD='password'

# Optionally, you can use a data source name (DSN)
# When using a DSN, you can remove the DB_NAME, DB_USER, DB_PASSWORD, and DB_HOST variables
# DATABASE_URL='mysql://database_user:database_password@database_host:database_port/database_name'

# Optional database variables
# DB_HOST is overridden to `database` by docker compose; set it for non-Docker installs.
# DB_HOST='localhost'
# DB_HOST='127.0.0.1' # Use if you encounter mysqli 2002 / socket connection issues
# DB_PREFIX='wp_'

WP_ENV='development'
WP_HOME='http://localhost:8080'
WP_SITEURL="${WP_HOME}/wp"

# Specify optional debug.log path
# WP_DEBUG_LOG='/path/to/debug.log'

# Redis (docker compose sets WP_REDIS_HOST=redis; leave unset to disable)
# WP_REDIS_HOST='redis'

# Generate your keys here: https://roots.io/salts.html
AUTH_KEY='generateme'
SECURE_AUTH_KEY='generateme'
LOGGED_IN_KEY='generateme'
NONCE_KEY='generateme'
AUTH_SALT='generateme'
SECURE_AUTH_SALT='generateme'
LOGGED_IN_SALT='generateme'
NONCE_SALT='generateme'
